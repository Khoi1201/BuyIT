import React from 'react'

const Checkout = () => {
  const column = [
    {
      title: 'Name',
      dataIndex: 'title',
      key: 'title',
    },
    {
      title: 'Price($)',
      dataIndex: 'price',
      key: 'price',
    },
  ]
  return <div></div>
}

export default Checkout
